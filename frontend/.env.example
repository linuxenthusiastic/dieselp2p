# Frontend (Vite). Solo claves públicas.
# Si dejas las variables de Supabase vacías, la app funciona en modo demo (login por rol sin contraseña).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# URL del backend Node.js. En desarrollo puede quedar vacía (usa el proxy de Vite hacia http://localhost:4000).
VITE_API_URL=
